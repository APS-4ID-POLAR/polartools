name: Docs

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Sphinx build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0  # hatch-vcs needs full history for version

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip

      - name: Install package + docs extras
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .[docs]

      - name: Build HTML docs
        # Don't pass -W: there are pre-existing numpydoc/section-format warnings
        # and a stale autosummary entry for polartools.hkl_utils. Fix those
        # separately, then re-add -W to make warnings fail the build.
        run: sphinx-build --keep-going -b html docs/source docs/build/html

      - uses: actions/upload-artifact@v4
        with:
          name: docs-html
          path: docs/build/html
